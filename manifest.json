{
  "manifest_version": 3,
  "name": "Daily Markdown Note",
  "version": "1.0.0",
  "description": "New tab becomes a daily Markdown journal stored locally. Uses chrome.storage.local only (on your device; no network upload; no cloud sync by default — local API only, not chrome.storage.sync).",
  "icons": {
    "16": "icons/icon16.png",
    "32": "icons/icon32.png",
    "48": "icons/icon48.png",
    "128": "icons/icon128.png"
  },
  "permissions": ["storage"],
  "chrome_url_overrides": {
    "newtab": "newtab.html"
  }
}
